detect_content:
  threshold: 30.0          # raise = fewer cuts; lower = more cuts
  min_scene_len: 150       # frames; ~5s @30fps
  luma_only: false
  kernel_size: 5
  downscale_factor: 2

split:
  output_ext: ".mp4"

trim_seconds:
  head: 0.00
  tail: 0.00
